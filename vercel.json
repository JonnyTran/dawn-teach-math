{
    "builds": [{
        "src": "api/wsgi.py",
        "use": "@vercel/python",
        "config": { 
            "maxLambdaSize": "100mb"
        }
    }],
    "rewrites": [
        { "source": "/api/:path*", "destination": "api/wsgi.py" }
    ]
}